# ── Live2D-Ai 环境变量配置 ──
# 复制此文件为 .env 或将变量导出到 shell 环境：
#   cp .env.example .env   (Python 端自动加载)
# 或手动设置环境变量 (Android CI 等场景)
#
# Android 构建也读取 local.properties，格式同此文件。

# DeepSeek API (对话 / 聊天)
# 从 https://platform.deepseek.com/api_keys 获取
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here

# 智谱 API (视觉 / 图像理解)
# 从 https://open.bigmodel.cn/usercenter/apikeys 获取
ZHIPU_API_KEY=your-zhipu-api-key-here

# 主对话 LLM 原生动作工具（live2d.perform_action）
# 设为 off 可整体关闭；默认开启。仅 OpenAI 兼容 provider 生效，
# 不支持的 provider 自动安全降级为纯文本对话。
# LIVE2DAI_NATIVE_TOOLS=off
