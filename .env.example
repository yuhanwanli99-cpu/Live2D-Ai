# ── Live2D-Ai 密钥真源（`.env`）──────────────────────────────────────────────
#
# **本文件是密钥的唯一真源**（rc.2 2026-09-12 用户口径）：
#   - 后端读它（优先级：`.env` > 进程环境 > 不鉴权）；
#   - 前端也能写它：设置面板里填 key → `PUT /api/v1/env` → 立即生效
#     （热重载，不用重启后端、不用重跑 ignite.sh）。
#
# 用法：
#   cp .env.example .env          # 然后填进你的真实 key
#   chmod 600 .env                # 权限收紧（前端写入时也会强制 0600）
#
# **`.env` 不进 git**（见 .gitignore），本示例文件只放键名占位。
# 键名从哪来：`live2d-ai.toml` 的 `[llm]` / `[tts]` 各自的 `api_key_env`。
# 想换名字就改配置里的 `api_key_env`，然后在这里加同名的一行。

# 对话 LLM（`live2d-ai.toml` → `[llm] api_key_env`，默认指向这个）。
# 任何 OpenAI 兼容端点都行：云端（如 https://api.deepseek.com/v1）或本地。
# 本地推理通常不需要 key —— 那就把 `[llm] api_key_env` 留空。
DEEPSEEK_API_KEY=sk-your-key-here

# 语音合成 TTS（`live2d-ai.toml` → `[tts] api_key_env`）。
# 本地 TTS 一般不需要 key；云端才填。
ZHIPU_API_KEY=your-key-here

# ── 可选：外部事件注入令牌（0.2.0-rc.1）─────────────────────────────────────
# `POST /api/v1/external/chat`（直播弹幕 / 礼物 / 本机脚本）的可选鉴权 token。
# 它不是任何 `api_key_env` 的目标，但同样由后端从 `.env` 快照读取。
#   - 设了它 → 外部请求必须带 body `token` 或 `Authorization: Bearer <token>`；
#   - 留空/删掉 → 回落到 Mod config 的 `token`；两者都空 = 不鉴权（仍只监听 127.0.0.1）。
# 契约与 sidecar 示例见 docs/external-input.md。
# EXTERNAL_INPUT_TOKEN=your-external-input-token
